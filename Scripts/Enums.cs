using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums
{
    public enum Direction { LEFT, RIGHT, UP, DOWN, STATIONARY };
    public enum MapTerrain { Field, Mountain};
    public enum Faction {Green, Yellow };
    public enum Resource { WATER, AMMO, TROOPS, FOOD}
}
